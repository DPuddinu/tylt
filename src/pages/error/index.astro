---
import ErrorMessage from '@components/ErrorMessage.astro';
type Props = {
  message: string;
};
const { message = 'Something went wrong!' } = Astro.props;
const redirect = Astro.url.searchParams.get('redirect');
---

<ErrorMessage fallbackUrl={`/${redirect}`} message={message} />
