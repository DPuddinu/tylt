---
import { SignIn } from 'auth-astro/components';
type Props = {
  label: string;
  className?: string;
};
const { className, label } = Astro.props;
---

<SignIn provider='github' options={{
  callbackUrl: '/goals'
}} class={`${className} rounded-2xl btn btn-primary`}>{label}</SignIn>
