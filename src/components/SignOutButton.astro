---
import { SignOut } from 'auth-astro/components';
type Props = {
  className?: string;
};
const { className = 'w-full text-base font-semibold text-center' } = Astro.props;
---

<SignOut class={className}>Logout</SignOut>
